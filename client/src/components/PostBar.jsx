import React, { useState } from "react";
import Voter from "./Voter";
import DeleteIcon from "@material-ui/icons/Delete";

const PostBar = (props) => {
  return <div>{/* <Voter postId={_id} creatorId={creatorId} /> */}</div>;
};

export default PostBar;
